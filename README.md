# command-run-terminal

## 脅威ハンティング可視化Webアプリ

Windows PCやADサーバなどの構成を可視化し、通信経路ごとに確認すべきイベントログやハンティング観点をポップアップで提示します。

### 起動方法

以下のどちらかの方法で静的サーバを起動できます。

```bash
python -m http.server 8000
```

または:

```bash
npm run dev
```

その後、ブラウザで `http://localhost:8000/` を開いてください。
